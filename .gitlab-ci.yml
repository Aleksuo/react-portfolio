default:
  image: node

build-job:
  stage: build
  script:
    - npm install -q --no-color --no-progress
    - npm run build

test-job:
  stage: test
  script:
    - npm test -- --coverage --watchAll=false

deploy-job:
  stage: deploy
  script:
    - echo "Deploy"
